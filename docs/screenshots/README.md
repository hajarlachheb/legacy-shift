# Demo screenshots for GitHub README

Place these four images in this folder so the main README demo section displays correctly:

| Filename | Content |
|----------|---------|
| `demo-parse.png` | Screenshot of the **Parse** tab (Java source + structural summary, classes/methods/imports). |
| `demo-explain.png` | Screenshot of the **Explain** tab (Java source + plain-English explanation). |
| `demo-tests.png` | Screenshot of the **Tests** tab (generated pytest suite, optionally with pass/fail status). |
| `demo-translated.png` | Screenshot of the **Translated** tab (Java source + Python translation, e.g. with “Partial” badge). |

Use the same screenshots you used for the demo (Parse, Explain, Tests, Translated views of the LegacyShift UI).
